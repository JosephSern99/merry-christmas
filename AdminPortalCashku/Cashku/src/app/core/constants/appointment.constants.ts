export enum AppointmentStatus {
    None = 0,
    Pending = 1,
    Approved = 2,
    Cancel = 3,
}

export enum AppointmentType {
    'Risk Tolerence' = 1,
    'Create Goal' = 2,
    'Plan Upgrade' = 3,
}